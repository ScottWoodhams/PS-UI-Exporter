import { CompDialogProps } from './ComponentDialog';
import React, { useState } from 'react';
import Spectrum, { Textarea } from 'react-uxp-spectrum';

export type logProps = { log: string };

export function Console({ log }: logProps) {

  return (
    <div>
        <sp-detail> ddd </sp-detail>
    </div>
  );
}
